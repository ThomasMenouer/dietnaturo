<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Facture</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

</head>
    <body>
        <h1>Facture</h1>
        <p>Référence Facture : {{ invoiceNumber }}</p>
        <p>Client : {{ order.firstname }} {{ order.lastname }} - {{ order.email }}</p>
        <p>Date : {{ order.createdAt|date('d/m/Y H:i') }}</p>

        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Produit</th>
                    <th scope="col">Quantité</th>
                    <th scope="col">Prix</th>
                </tr>
            </thead>
            <tbody>
                {% for detail in order.orderDetails %}
                    <tr>
                        <th scope="row">{{ detail.productName }}</td>
                        <td>{{ detail.quantity }}</td>
                        <td>{{ (detail.price / 100)|number_format(2, ',', '.') }} €</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        <p><strong>Total :</strong> {{ (order.totalPrice / 100)|number_format(2, ',', '.') }} €</p>
    </body>
</html>